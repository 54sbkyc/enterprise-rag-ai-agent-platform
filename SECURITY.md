# 安全说明

本文档说明本项目作为 AI 应用开发作品集时的安全边界和发布注意事项。

## 支持范围

当前项目用于本地演示、毕业设计升级和 GitHub 作品集展示。默认配置不需要真实 API Key，可以使用本地抽取式回答完成演示。

正式发布到 GitHub 前，请先阅读：

- [GitHub 发布清单](docs/github_release_checklist.md)
- [最终验收报告](docs/final_acceptance_report.md)
- [架构决策记录](docs/architecture_decisions.md)

## 不要提交真实密钥

不要提交真实密钥、私有配置或生产环境凭证，包括但不限于：

- `.env`
- `.env.*`
- API Key
- 数据库连接串
- 内网地址
- 临时公网隧道地址

仓库只保留 `.env.example`，用于说明可配置项。

## 本地运行数据

以下本地运行数据不进入公开仓库：

- `backend/data/*.db`
- `backend/data/uploads/`
- `.runtime/`
- `.pytest_cache/`
- `__pycache__/`
- `tools/`
- `start_tunnel.ps1`
- `tunnel-url.txt`

这些文件可能包含本地测试数据、上传资料、运行日志或临时工具，不适合作为作品集内容公开。

## AI 安全边界

项目已经包含几类基础 AI 安全控制：

- Prompt 注入检测：对“忽略规则”“输出全部内部资料”等危险意图进行拦截。
- 权限控制：管理员和普通员工角色隔离，检索范围按用户权限过滤。
- 文档密级：普通员工不能检索高密级文档片段。
- 敏感信息脱敏：回答和日志中尽量避免暴露敏感字段。
- 审计日志：记录问答、拦截原因、管理操作和文档变更。
- 会话有效期：登录会话默认 12 小时过期，过期 Token 会在访问时失效并清理。
- 上传限额：单文件默认不超过 10 MB，超限返回 `413`，不保留半成品文件或数据库记录。
- 跨域白名单：同源部署默认不启用 CORS；跨域来源必须显式配置，禁止 `*`。
- 降级透明：模型调用失败后可本地降级，但观测数据会记录生成方式和失败类别，不伪装为模型调用成功。

这些能力适合作为 AI 应用安全意识展示。生产环境仍需要更完整的访问控制、密钥管理、日志平台、异常告警和人工审批机制。

## 漏洞反馈

如果你在本地使用或面试演示时发现安全问题，建议按以下信息记录：

- 复现步骤
- 使用的账号角色
- 输入问题或上传文件类型
- 期望结果和实际结果
- 是否涉及越权、敏感信息泄露或 Prompt 注入绕过

修复安全问题时，请先补充对应测试，再修改实现，最后运行总验收：

```powershell
.\scripts\verify_project.ps1
```
